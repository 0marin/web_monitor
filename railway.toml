[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300

[deploy.envs]
NODE_ENV = "production"
TZ = "Europe/Kiev"